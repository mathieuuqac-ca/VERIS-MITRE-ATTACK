# Dépendances à ajouter pour la semaine 5
scikit-learn>=1.3.0
joblib>=1.3.0

# Optionnel mais recommandé pour la journalisation d'expériences.
# Si MLflow pose problème, le code continue avec un journal local JSON.
mlflow>=2.10.0
